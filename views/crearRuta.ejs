<!DOCTYPE HTML>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>SendaSur - Crear Ruta</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="/imagenes/favicon.png" type="image/png">

    <style>
      :root {
        overflow-x: hidden;
      }
      body {
        background-image: url("/Imagenes/2T.jpg");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        margin: 0;
      }

      .contenedor-form {
        background-color: rgba(255, 255, 255, 0.85);
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        width: 100%;
        max-width: 700px;
      }

      .contenedor-form label,
      .contenedor-form input,
      .contenedor-form textarea,
      .contenedor-form select,
      .contenedor-form button {
        font-size: 1.1rem;
      }

    
    </style>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-success sticky-top">
      <div class="container-fluid">
        <a href="/inicio"><img src="/Imagenes/favicon.png" class="mx-4 img-fluid" alt="Logo" style="height: 40px;"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item"><a class="nav-link" href="/inicio">Inicio</a></li>
            <li class="nav-item"><a class="nav-link active" href="/rutas">Rutas</a></li>
            <li class="nav-item"><a class="nav-link" href="/mis-rutas">Mis Rutas</a></li>
            <li class="nav-item"><a class="nav-link" href="/mis-comentarios">Mis comentarios</a></li>
          </ul>
          <ul class="navbar-nav ms-auto">
            <% if (usuario) { %>
              <li class="nav-item">
                <span class="nav-link">Hola, <strong><%= usuario.nombreUsuario %></strong></span>
              </li>
              <li class="nav-item">
                <a class="nav-link btn btn-danger text-white" href="/logout">Logout</a>
              </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Formulario -->
    <div class="container my-5">
      <div class="d-flex justify-content-center">
        <div class="contenedor-form">
          <h2 class="text-center mb-4">Crear Nueva Ruta</h2>
          <form action="/crear-ruta" method="POST">
            <div class="mb-3">
              <label class="form-label">Nombre de la Ruta</label>
              <input type="text" class="form-control" name="nombreRuta" required>
            </div>

            <div class="mb-3">
              <label class="form-label">Descripción</label>
              <textarea class="form-control" name="descripcionRuta" rows="4" required></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">Localización</label>
              <input type="text" class="form-control" name="localizacionRuta" placeholder="Ej: Sierra de Grazalema(Google Maps)" required>
            </div>
            

            <div class="mb-3">
              <label class="form-label">Dificultad</label>
              <select class="form-select" name="dificultadRuta" required>
                <option value="Facil">Fácil</option>
                <option value="Media">Media</option>
                <option value="Difícil">Difícil</option>
              </select>
            </div>

            <div class="mb-3">
              <label class="form-label">Valoración (1-5)</label>
              <input type="number" class="form-control" name="valoracionRuta" min="1" max="5">
            </div>

            <div class="mb-3">
              <label class="form-label">Imagen (URL)</label>
              <input type="text" class="form-control" name="imagenRuta" placeholder="Copiar vínculo de imagen">
            </div>

            <div class="text-center">
              <button type="submit" class="btn btn-success">Guardar Ruta</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-light py-4 mt-5">
      <div class="container text-center">
        <p class="mb-2">Página web creada por Carlos Miguel García Morillo</p>
        <a href="/rutas" class="btn btn-primary">Volver a las Rutas</a>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
