<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Editar Ruta</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" href="/imagenes/favicon.png" type="image/png">
  <style>
    html, body {
      height: 100%;
    }

    body {
      background-image: url("/Imagenes/2T.jpg");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      margin: 0;
      padding: 0;
    }

  </style>
</head>
<body>
  <div class="container mt-5">
    <h2>Editar Ruta</h2>
    <form action="/rutas/editar/<%= ruta.idRuta %>" method="POST">
      <div class="mb-3">
        <label>Nombre</label>
        <input type="text" name="nombreRuta" value="<%= ruta.nombreRuta %>" class="form-control" required>
      </div>
      <div class="mb-3">
        <label>Descripción</label>
        <textarea name="descripcionRuta" class="form-control" required><%= ruta.descripcionRuta %></textarea>
      </div>
      <div class="mb-3">
        <label>Localización</label>
        <input type="text" name="localizacionRuta" value="<%= ruta.localizacionRuta %>" class="form-control">
      </div>
      <div class="mb-3">
        <label>Dificultad</label>
        <select name="dificultadRuta" class="form-control">
          <option value="Facil" <%= ruta.dificultadRuta === 'Facil' ? 'selected' : '' %>>Fácil</option>
          <option value="Media" <%= ruta.dificultadRuta === 'Media' ? 'selected' : '' %>>Media</option>
          <option value="Difícil" <%= ruta.dificultadRuta === 'Difícil' ? 'selected' : '' %>>Difícil</option>
        </select>
      </div>
      <div class="mb-3">
        <label>URL de imagen</label>
        <input type="text" name="imagenRuta" value="<%= ruta.imagenRuta %>" class="form-control">
      </div>
      <div class="mb-3">
        <label>Valoración (1-5)</label>
        <input type="number" name="valoracionRuta" value="<%= ruta.valoracionRuta %>" min="1" max="5" class="form-control">
      </div>
      <button type="submit" class="btn btn-success">Guardar Cambios</button>
      <a href="/mis-rutas" class="btn btn-secondary">Cancelar</a>
    </form>
  </div>
</body>
</html>
